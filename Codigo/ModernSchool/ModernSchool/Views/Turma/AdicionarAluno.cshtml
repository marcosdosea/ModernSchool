@model ModernSchoolWEB.Models.AlunoTurmaViewModel

<h4>AlunoTurmaViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AdicionarAluno">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdAluno" class="control-label"></label>
                <select asp-for="IdAluno" class="form-control" asp-items="@Model.listaAluno" ></select>
                <span asp-validation-for="IdAluno" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdTurma" class="control-label"></label>
                <input asp-for="IdTurma" class="form-control" value="@Model.IdTurma" />
                <span asp-validation-for="IdTurma" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
